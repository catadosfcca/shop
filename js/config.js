/* ============================================================
   CONFIG DO FORMULÁRIO — edite só esta parte quando precisar
   trocar o Google Forms ou os IDs dos campos.

   Veja o passo a passo completo em README.md ("Como configurar
   o Google Forms").
   ============================================================ */

const FORM_CONFIG = {
  // Link base do formulário, terminando em /viewform
  FORM_BASE_URL: "https://docs.google.com/forms/d/e/1FAIpQLSfOUMWc2c-nBkgV2WL2xN5TTWjnQuTZSIgn5Cq0y7ArpxcAQA/viewform",

  // ID do campo "Produto" no Forms (o número depois de "entry." no link
  // de pré-preenchimento). Deixe "" se ainda não tiver.
  FORM_ENTRY_PRODUTO: "956172927",

  // ID do campo "Tamanho" no Forms. Deixe "" se ainda não tiver ou se
  // não quiser travar um valor padrão (o comprador escolhe na hora).
  FORM_ENTRY_TAMANHO: "",
};
